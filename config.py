# config.py

# Aqui você pode definir as credenciais do banco de dados
DATABASE_URI = 'postgresql+psycopg2://nexway:qQLVpb39@localhost/bd_nexway'

# 'username' é o seu nome de usuário para o banco de dados PostgreSQL
# 'password' é a senha para o banco de dados
# 'hostname' é o endereço do servidor onde o banco de dados está hospedado (pode ser 'localhost' se for local)
# 'databasename' é o nome do seu banco de dados
